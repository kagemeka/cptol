#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  double l; cin >> l;
  cout << setprecision(10) << pow(l / 3, 3) << '\n';
  return 0;
}
