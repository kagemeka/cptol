const fs = require('fs');

function int(n) {
  return parseInt(n, 10);
}

function main(input) {
  s = input.trim();
  console.log(s + 'pp');
}

input = fs.readFileSync('/dev/stdin', 'utf8');
main(input);
